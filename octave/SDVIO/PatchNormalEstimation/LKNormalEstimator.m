classdef LKNormalEstimator
    %LUCASKANADENORMALESTIMATOR Uses a lucas kanade / direct method to
    %estimate the direction of the patch normal.
    
    properties
        
    end
    
    methods
        function obj = LKNormalEstimator(sourceKF, sourcePixel, targetKFVector, targetPixelVector)
            % Assume that the pixel vector is a 2xN matrix where each
            % column is a pixel.
        end
        
    end
end

